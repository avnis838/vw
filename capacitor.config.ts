import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Vw.app',
  appName: 'Vw',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
